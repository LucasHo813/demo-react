import React from 'react'

export default function Title({mainTitle}) {
    return (
        <div>
            <h1 style={{textAlign: 'center'}}>
                {mainTitle}
            </h1>
        </div>
    )
}
